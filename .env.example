NODE_ENV=production
MONGODB_URI=mongodb://scrollyboard-mongodb:27017/scrollyboard
PORT=5173
HOST=0.0.0.0

# Trust ngrok's forwarded headers so adapter-node resolves the real origin.
PROTOCOL_HEADER=x-forwarded-proto
HOST_HEADER=x-forwarded-host
